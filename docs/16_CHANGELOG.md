# CHANGELOG

## 2026-08-04 — повноекранна навігація між секціями

- Прибрано горизонтальний скрол сторінки на рівні `html`, `body` і `main`.
- Усі поточні й майбутні прямі секції `main` займають щонайменше висоту екрана.
- Додано обов'язковий вертикальний scroll-snap для колеса миші та touch-свайпу; desktop-перехід між межами секцій плавно виконується через GSAP.
- Горизонтальний свайп мобільних карток `catalog-preview` ізольовано від прокручування сторінки.

## 2026-08-04 — блок вибору категорії маринадів

- Реалізовано секцію `catalog-preview` за `CATALOG_PREVIEW_SPEC.md`.
- Створено шість оптимізованих WebP-шарів для рідкої та сухої сцен без тари й упаковки.
- Додано desktop-композицію з двох колонок, вертикальний tablet-режим та інерційний mobile-swipe.
- Реалізовано GSAP-паралакс, відблиски води, рух диму, анімацію входу у майбутній каталог і зворотне повернення.
- Картки товарів не створювалися; під час переходу показується лише трьохшаровий фон.

## 2026-08-04 — hover номера телефону

- У Header hover номера не змінює фон посилання.
- Помаранчева копія іконки та цифр відкривається маскою зліва направо за `0.5s`, тому колір заповнює лише їхні контури.

## 2026-08-04 — розмиття Header

- Значення `backdrop-filter` для Header змінено на `blur(4px)` разом із WebKit-префіксом.

## 2026-08-04 — повний логотип у Header

- Круглий crop логотипа у Header прибрано.
- Оригінальний SVG `206×60` тепер показується повністю: знак разом із фірмовим написом SpiceLand.
- Додано адаптивну ширину логотипа від mobile до надшироких екранів без спотворення пропорцій.

## 2026-08-04 — глобальний hover кнопок і об’ємне обертання логотипа

- Для всіх активних кнопок додано єдиний hover: заливка `#FF9100` рухається зліва направо за `0.5s`, текст стає `#403536`.
- Після завершення loader-анімації Hero-логотип знову обертається у 3D через GSAP.
- До оригінального логотипа додано окрему багатошарову кромку, задню сторону та перспективу, щоб медальйон залишався об’ємним і видимим у профіль.

## 2026-08-04 — уточнена послідовність loader-анімації

- Краплі летять точно назустріч одна одній протягом `3s`, стикаються, зменшуються і лише після цього падають на логотип.
- Усі анімовані обертання прибрано з крапель, фази оживлення та постійної анімації Hero-логотипа.
- Сірий loader-логотип і фінальний кольоровий Hero-логотип мають однакові координати та розміри на кожному адаптивному брейкпоінті.
- Desktop-кнопки Hero підняті ще на `40px`: нижній відступ змінено зі `100px` на `140px`.

## 2026-08-04 — форма крапель і видимість логотипа

- На mobile і tablet відстань між елементами `header__actions` встановлена в `14px`.
- Desktop-кнопки Hero підняті на `40px`: нижній відступ змінено з `60px` на `100px`.
- Обертання Hero-логотипа змінено: повне обертання виконується по осі Z, а 3D-нахил обмежений до ±12°, тому логотип не зникає в положенні ребром.
- Рідкі потоки замінено на округлі об’ємні краплі з короткими витягнутими хвостами.
- Після зіткнення краплі зменшуються, підстрибують і падають на відповідні частини логотипа.
- Золоте кільце з фази оживлення логотипа видалено.

## 2026-08-04 — Header і обмеження Hero-контейнера

- Кнопка «Залишити заявку» повернена в Header на всіх ширинах, включно з `320px`.
- Видалено локальне `width: 100%`, яке перекривало глобальне обмеження Hero-контейнера.
- Внутрішній контент Hero знову обмежений шириною `1440px` і обов’язковими відступами по `2px`; фон залишається повноширинним.

## 2026-08-04 — позиционирование кнопок Hero

- На мобильных устройствах ширина каждой кнопки Hero установлена в `260px`.
- Нижний отступ Hero на mobile и tablet увеличен на `40px`.
- На desktop блок кнопок перемещён в левый нижний угол контейнера с отступами `60px` слева и снизу.

## 2026-08-04 — уточнение контейнеров и пластики потоков

- Правило контейнера уточнено: оно обязательно для всех секций и всего смыслового контента без исключений.
- Для каждого блока закреплена структура «полноширинный фон + внутренний контейнер максимум `1440px` с отступами `2px`».
- Логотип Hero перемещён в верхний центр на `20px` ниже Header.
- Жидкие потоки получили непрерывную змееобразную GSAP-деформацию через `skew`, неравномерный `scale`, повороты и смещения.
- Продолжительность фазы подлёта потоков установлена ровно в `2s`.

## 2026-08-03 — единый контейнер и корректировка интро

- Максимальная ширина внутреннего контента установлена в `1440px`, минимальные боковые отступы — `2px`.
- Фон Header вынесен на всю ширину экрана, внутреннее содержимое оставлено в общем контейнере.
- Логотип Hero закреплён в геометрическом центре экрана на всех контрольных ширинах.
- Чёрный фон трёх жидких потоков преобразован в прозрачный alpha-канал.
- Фаза движения капель увеличена на `0.3s` через GSAP.

## 2026-08-03 — редизайн за фінальним референсом

- Світлий природний Hero замінено на темну гастрономічну сцену з референса.
- Створено три окремі растрові паралакс-шари: фон, середній план і передній план.
- Вступну анімацію перероблено під фотореалістичні потоки трьох маринадів.
- Затверджено послідовність: зіткнення рідин, розліт до секторів сірого логотипа, повернення фірмових кольорів, проявлення Hero.

## 2026-08-03 — Header та Hero за новою специфікацією

- Реалізовано тільки Header, Hero, вступну анімацію та пов’язані модальні вікна згідно з `HEADER_HERO_SPEC.md`.
- Додано фіксований glassmorphism Header і адаптивне burger-меню.
- Додано природний ранковий фон Hero, три декоративні шари та паралакс.
- Анімації loader, логотипа, меню, модальних вікон і паралаксу реалізовано через GSAP.
- Додано mobile-first адаптацію від 320px до 1920px і більше.
- Telegram-відправлення залишено окремим майбутнім модулем згідно зі специфікацією.

## 2026-08-03 — перезапуск проекта

- Предыдущая реализация полностью удалена по решению владельца проекта.
- Сохранены проектная документация и оригинальный логотип SpiceLand.
- Новую реализацию начать с чистой основы и вести строго по документации.

## 2026-08-03

- Создана базовая структура Vite/SCSS-проекта.
- Реализованы адаптивные Header, Hero и карточки продуктов.
- Подключён оригинальный логотип SpiceLand.
- Добавлены GSAP-анимации с поддержкой `prefers-reduced-motion`.
- Зафиксированы правила mobile-first от 320px до сверхшироких экранов.
- Круглый знак SpiceLand установлен как favicon по стабильному адресу `/favicon.svg` для браузеров и Google Search.
